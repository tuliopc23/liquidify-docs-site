---
title: Slider
description: An interactive slider component for selecting numerical values within a specified range.
---

# Slider

An interactive slider component for selecting numerical values within a specified range.

## Installation

```bash
bun add liquidify-react
```

## Import

```tsx
import { Slider } from "liquidify-react/slider"
```

## Basic Usage

Create a simple slider for value selection:

```tsx
import { Slider } from "liquidify-react/slider"

export function BasicExample() {
  return (
    <Slider.Root defaultValue={[50]} min={0} max={100}>
      <Slider.Label>Volume</Slider.Label>
      <Slider.ValueText />
      <Slider.Control>
        <Slider.Track>
          <Slider.Range />
        </Slider.Track>
        <Slider.Thumb index={0} />
      </Slider.Control>
    </Slider.Root>
  )
}
```

## Controlled Slider

Control the slider value with state:

```tsx
import { useState } from "react"
import { Slider } from "liquidify-react/slider"

export function ControlledExample() {
  const [value, setValue] = useState([25])

  return (
    <Slider.Root 
      value={value} 
      onValueChange={(details) => setValue(details.value)}
      min={0} 
      max={100}
    >
      <Slider.Label>Price Range ($)</Slider.Label>
      <Slider.ValueText />
      <Slider.Control>
        <Slider.Track>
          <Slider.Range />
        </Slider.Track>
        <Slider.Thumb index={0} />
      </Slider.Control>
      <p>Selected value: ${value[0]}</p>
    </Slider.Root>
  )
}
```

## Range Slider

Create a dual-thumb slider for selecting ranges:

```tsx
import { useState } from "react"
import { Slider } from "liquidify-react/slider"

export function RangeExample() {
  const [range, setRange] = useState([20, 80])

  return (
    <Slider.Root 
      value={range} 
      onValueChange={(details) => setRange(details.value)}
      min={0} 
      max={100}
    >
      <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center" }}>
        <Slider.Label>Price Range</Slider.Label>
        <div style={{ fontSize: "14px", color: "#6b7280" }}>
          ${range[0]} - ${range[1]}
        </div>
      </div>
      <Slider.Control>
        <Slider.Track>
          <Slider.Range />
        </Slider.Track>
        <Slider.Thumb index={0} />
        <Slider.Thumb index={1} />
      </Slider.Control>
    </Slider.Root>
  )
}
```

## Step Values

Use discrete steps for value selection:

```tsx
import { Slider } from "liquidify-react/slider"

export function StepExample() {
  return (
    <Slider.Root defaultValue={[5]} min={0} max={10} step={1}>
      <Slider.Label>Rating (1-10)</Slider.Label>
      <Slider.ValueText />
      <Slider.Control>
        <Slider.Track>
          <Slider.Range />
        </Slider.Track>
        <Slider.Thumb index={0} />
      </Slider.Control>
      <div style={{ display: "flex", justifyContent: "space-between", marginTop: "8px", fontSize: "12px", color: "#6b7280" }}>
        {Array.from({ length: 11 }, (_, i) => (
          <span key={i}>{i}</span>
        ))}
      </div>
    </Slider.Root>
  )
}
```

## Vertical Slider

Create a vertical orientation slider:

```tsx
import { Slider } from "liquidify-react/slider"

export function VerticalExample() {
  return (
    <div style={{ height: "200px", display: "flex", alignItems: "center", gap: "20px" }}>
      <Slider.Root defaultValue={[75]} min={0} max={100} orientation="vertical">
        <Slider.Label>Temperature</Slider.Label>
        <Slider.Control style={{ height: "150px" }}>
          <Slider.Track>
            <Slider.Range />
          </Slider.Track>
          <Slider.Thumb index={0} />
        </Slider.Control>
        <Slider.ValueText />
      </Slider.Root>
      
      <div style={{ fontSize: "12px", color: "#6b7280" }}>
        <div>100°F</div>
        <div style={{ marginTop: "120px" }}>0°F</div>
      </div>
    </div>
  )
}
```

## Color Picker Sliders

Use sliders for color value selection:

```tsx
import { useState } from "react"
import { Slider } from "liquidify-react/slider"

export function ColorPickerExample() {
  const [red, setRed] = useState([128])
  const [green, setGreen] = useState([128])
  const [blue, setBlue] = useState([128])

  const rgbColor = `rgb(${red[0]}, ${green[0]}, ${blue[0]})`

  return (
    <div>
      <div style={{ 
        width: "100px", 
        height: "100px", 
        backgroundColor: rgbColor,
        border: "1px solid #e5e7eb",
        borderRadius: "8px",
        marginBottom: "20px"
      }} />
      
      <div style={{ display: "flex", flexDirection: "column", gap: "16px" }}>
        <Slider.Root 
          value={red} 
          onValueChange={(details) => setRed(details.value)}
          min={0} 
          max={255}
        >
          <Slider.Label style={{ color: "#ef4444" }}>Red: {red[0]}</Slider.Label>
          <Slider.Control>
            <Slider.Track>
              <Slider.Range style={{ backgroundColor: "#ef4444" }} />
            </Slider.Track>
            <Slider.Thumb index={0} />
          </Slider.Control>
        </Slider.Root>

        <Slider.Root 
          value={green} 
          onValueChange={(details) => setGreen(details.value)}
          min={0} 
          max={255}
        >
          <Slider.Label style={{ color: "#10b981" }}>Green: {green[0]}</Slider.Label>
          <Slider.Control>
            <Slider.Track>
              <Slider.Range style={{ backgroundColor: "#10b981" }} />
            </Slider.Track>
            <Slider.Thumb index={0} />
          </Slider.Control>
        </Slider.Root>

        <Slider.Root 
          value={blue} 
          onValueChange={(details) => setBlue(details.value)}
          min={0} 
          max={255}
        >
          <Slider.Label style={{ color: "#3b82f6" }}>Blue: {blue[0]}</Slider.Label>
          <Slider.Control>
            <Slider.Track>
              <Slider.Range style={{ backgroundColor: "#3b82f6" }} />
            </Slider.Track>
            <Slider.Thumb index={0} />
          </Slider.Control>
        </Slider.Root>
      </div>
      
      <p style={{ marginTop: "16px", fontFamily: "monospace" }}>
        {rgbColor}
      </p>
    </div>
  )
}
```

## Disabled Slider

Show a disabled slider component:

```tsx
import { Slider } from "liquidify-react/slider"

export function DisabledExample() {
  return (
    <Slider.Root defaultValue={[60]} min={0} max={100} disabled>
      <Slider.Label>Disabled Slider</Slider.Label>
      <Slider.ValueText />
      <Slider.Control>
        <Slider.Track>
          <Slider.Range />
        </Slider.Track>
        <Slider.Thumb index={0} />
      </Slider.Control>
      <p style={{ color: "#6b7280", fontSize: "14px" }}>
        This slider cannot be modified
      </p>
    </Slider.Root>
  )
}
```

## Custom Formatting

Format slider values with custom display:

```tsx
import { useState } from "react"
import { Slider } from "liquidify-react/slider"

export function FormattedExample() {
  const [value, setValue] = useState([50])
  
  const formatValue = (val) => {
    if (val < 25) return "Low"
    if (val < 75) return "Medium"
    return "High"
  }

  return (
    <Slider.Root 
      value={value} 
      onValueChange={(details) => setValue(details.value)}
      min={0} 
      max={100}
    >
      <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center" }}>
        <Slider.Label>Intensity Level</Slider.Label>
        <div style={{ fontWeight: "600" }}>
          {formatValue(value[0])} ({value[0]}%)
        </div>
      </div>
      <Slider.Control>
        <Slider.Track>
          <Slider.Range />
        </Slider.Track>
        <Slider.Thumb index={0} />
      </Slider.Control>
      <div style={{ display: "flex", justifyContent: "space-between", marginTop: "8px", fontSize: "12px", color: "#6b7280" }}>
        <span>Low</span>
        <span>Medium</span>
        <span>High</span>
      </div>
    </Slider.Root>
  )
}
```

## API Reference

### Slider.Root

The root container for the slider component.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `number[]` | - | Controlled slider values |
| `defaultValue` | `number[]` | `[0]` | Default slider values |
| `onValueChange` | `(details) => void` | - | Callback when value changes |
| `min` | `number` | `0` | Minimum slider value |
| `max` | `number` | `100` | Maximum slider value |
| `step` | `number` | `1` | Step increment for values |
| `orientation` | `"horizontal" \| "vertical"` | `"horizontal"` | Slider orientation |
| `disabled` | `boolean` | `false` | Whether the slider is disabled |
| `invalid` | `boolean` | `false` | Whether the slider is invalid |

### Slider.Label

The label for the slider component.

### Slider.ValueText

Displays the current slider value(s).

### Slider.Control

Container for the track, range, and thumbs.

### Slider.Track

The background track of the slider.

### Slider.Range

The filled portion indicating the selected range.

### Slider.Thumb

Individual draggable thumb for value selection.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `index` | `number` | - | Index of the thumb (required) |

## Accessibility

The Slider component follows WAI-ARIA guidelines:

- Uses `role="slider"` for each thumb
- Implements `aria-valuemin`, `aria-valuemax`, and `aria-valuenow`
- Supports keyboard navigation with arrow keys
- Provides proper focus management
- Associates labels with `aria-labelledby`
- Supports Home/End keys for min/max values
- Implements Page Up/Down for larger increments

## Best Practices

- Provide clear labels indicating what the slider controls
- Use appropriate min/max values and step increments
- Show current values to provide immediate feedback
- Consider using range sliders for selecting ranges
- Ensure sufficient contrast for focus and active states
- Test keyboard navigation and screen reader compatibility
- Use consistent styling across your application
- Consider touch targets for mobile interfaces
