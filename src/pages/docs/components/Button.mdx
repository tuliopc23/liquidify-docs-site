---
layout: ../../../layouts/DocsLayout.astro
---

import { Button } from "liquidify-react/button"
import { ComponentShowcase, SimpleShowcase } from "../../../components/ComponentShowcase"

# Button

Interactive elements that trigger actions when clicked. Essential for forms, navigation, and user interactions with multiple variants for different contexts.

## Import

```tsx
import { Button } from "liquidify-react/button"
```

## Basic Usage

<ComponentShowcase
  title="Button Variants"
  description="Different button styles for various use cases"
  code={`<div style={{ display: "flex", gap: "1rem" }}>
  <Button variant="primary">Primary</Button>
  <Button variant="secondary">Secondary</Button>
  <Button variant="ghost">Ghost</Button>
</div>`}
  client:load
>
  <Button variant="primary" client:load>Primary</Button>
  <Button variant="secondary" client:load>Secondary</Button>
  <Button variant="ghost" client:load>Ghost</Button>
</ComponentShowcase>

```tsx title="Button.tsx"
import { Button } from "liquidify-react/button"

export default function Example() {
  return (
    <div style={{ display: "flex", gap: "1rem" }}>
      <Button variant="primary">Primary</Button>
      <Button variant="secondary">Secondary</Button>
      <Button variant="ghost">Ghost</Button>
    </div>
  )
}
```

## Sizes

<ComponentShowcase
  title="Button Sizes"
  description="Different sizes for various interface contexts"
  code={`<div style={{ display: "flex", gap: "1rem", alignItems: "center" }}>
  <Button size="sm">Small</Button>
  <Button size="md">Medium</Button>
  <Button size="lg">Large</Button>
</div>`}
  client:load
>
  <Button size="sm" client:load>Small</Button>
  <Button size="md" client:load>Medium</Button>
  <Button size="lg" client:load>Large</Button>
</ComponentShowcase>

## With Icons

```tsx
export default function IconsExample() {
	return (
		<div style={{ display: "flex", gap: "1rem" }}>
			<Button>
				<PlusIcon />
				Add Item
			</Button>
			
			<Button variant="secondary">
				Download
				<DownloadIcon />
			</Button>
			
			<Button variant="ghost" size="sm">
				<SearchIcon />
			</Button>
		</div>
	)
}
```

## States

<ComponentShowcase
  title="Button States"
  description="Different interaction states for buttons"
  code={`<div style={{ display: "flex", gap: "1rem" }}>
  <Button>Default</Button>
  <Button disabled>Disabled</Button>
</div>`}
  client:load
>
  <Button client:load>Default</Button>
  <Button disabled client:load>Disabled</Button>
</ComponentShowcase>

## Button Variants

<ComponentShowcase
  title="All Button Variants"
  description="Complete set of available button variants"
  code={`<div style={{ display: "flex", gap: "1rem", flexWrap: "wrap" }}>
  <Button variant="primary">Primary</Button>
  <Button variant="secondary">Secondary</Button>
  <Button variant="ghost">Ghost</Button>
  <Button variant="danger">Danger</Button>
  <Button variant="success">Success</Button>
  <Button variant="warning">Warning</Button>
</div>`}
  client:load
>
  <Button variant="primary" client:load>Primary</Button>
  <Button variant="secondary" client:load>Secondary</Button>
  <Button variant="ghost" client:load>Ghost</Button>
  <Button variant="danger" client:load>Danger</Button>
  <Button variant="success" client:load>Success</Button>
  <Button variant="warning" client:load>Warning</Button>
</ComponentShowcase>

## Sizes

<ComponentShowcase
  title="All Button Sizes"
  description="Complete set of available button sizes"
  code={`<div style={{ display: "flex", gap: "1rem", alignItems: "center" }}>
  <Button size="sm">Small</Button>
  <Button size="md">Medium</Button>
  <Button size="lg">Large</Button>
  <Button size="xl">Extra Large</Button>
</div>`}
  background="light"
  client:load
>
  <Button size="sm" client:load>Small</Button>
  <Button size="md" client:load>Medium</Button>
  <Button size="lg" client:load>Large</Button>
  <Button size="xl" client:load>Extra Large</Button>
</ComponentShowcase>

## API Reference

### Props
- `variant`: `"primary" | "secondary" | "ghost" | "danger" | "success" | "warning"` - Visual style variant (optional)
- `size`: `"sm" | "md" | "lg" | "xl"` - Button size (optional)
- `disabled`: `boolean` - Disable button interaction (inherited from HTML button)
- `children`: `React.ReactNode` - Button content (required)
- Standard HTML button attributes are supported

### Accessibility
- Full keyboard navigation with Enter and Space
- Disabled state properly announced
- Loading state with aria-busy
- Focus management and visual indicators
