---
title: "ProgressLinear"
description: "Linear progress bar component for showing completion status"
---

# ProgressLinear

A linear progress bar component that displays completion status with smooth animations. Perfect for file uploads, form completion, step-by-step processes, or any scenario requiring visual progress feedback in a horizontal bar format.

## Installation

```bash
npm install @liquidify/components
```

## Import

```tsx
import { ProgressLinear } from "@liquidify/components/progressLinear";
```

## Usage

### Basic Progress Linear

```tsx
import { useState } from "react";
import { ProgressLinear } from "@liquidify/components/progressLinear";

export default function BasicProgressLinear() {
  const [progress, setProgress] = useState(0);

  const incrementProgress = () => {
    setProgress(prev => Math.min(100, prev + 15));
  };

  const resetProgress = () => {
    setProgress(0);
  };

  return (
    <div style={{ padding: "2rem", maxWidth: "400px", margin: "0 auto" }}>
      <h3>Task Progress</h3>

      <ProgressLinear value={progress} label={`${progress}% Complete`} />

      <div style={{ marginTop: "2rem", display: "flex", gap: "1rem" }}>
        <button onClick={incrementProgress} disabled={progress >= 100}>
          Advance Progress
        </button>
        <button onClick={resetProgress}>
          Reset
        </button>
      </div>

      <p style={{ marginTop: "1rem", color: "#666", textAlign: "center" }}>
        Current progress: {progress}%
      </p>
    </div>
  );
}
```

### File Upload with Progress

```tsx
import { useState, useEffect } from "react";
import { ProgressLinear } from "@liquidify/components/progressLinear";

export default function FileUploadProgress() {
  const [uploadProgress, setUploadProgress] = useState(0);
  const [isUploading, setIsUploading] = useState(false);
  const [uploadedFile, setUploadedFile] = useState(null);

  const simulateUpload = (file) => {
    setIsUploading(true);
    setUploadProgress(0);
    setUploadedFile(file);

    const interval = setInterval(() => {
      setUploadProgress(prev => {
        if (prev >= 100) {
          clearInterval(interval);
          setIsUploading(false);
          return 100;
        }
        return prev + Math.random() * 12;
      });
    }, 200);
  };

  const handleFileSelect = (event) => {
    const file = event.target.files[0];
    if (file) {
      simulateUpload(file);
    }
  };

  return (
    <div style={{ padding: "2rem", maxWidth: "500px", margin: "0 auto" }}>
      <h3>File Upload</h3>

      <div style={{ marginBottom: "2rem" }}>
        <input
          type="file"
          onChange={handleFileSelect}
          disabled={isUploading}
          style={{ marginBottom: "1rem" }}
        />

        {isUploading && (
          <div>
            <div style={{ display: "flex", justifyContent: "space-between", marginBottom: "0.5rem" }}>
              <span>Uploading {uploadedFile?.name}</span>
              <span>{Math.round(uploadProgress)}%</span>
            </div>
            <ProgressLinear value={uploadProgress} />
          </div>
        )}
      </div>

      {uploadedFile && !isUploading && uploadProgress === 100 && (
        <div style={{ padding: "1rem", backgroundColor: "#d4edda", borderRadius: "8px", textAlign: "center" }}>
          <h4 style={{ color: "#155724", margin: 0 }}>✅ Upload Complete!</h4>
          <p style={{ color: "#155724", marginTop: "0.5rem" }}>
            {uploadedFile.name} has been successfully uploaded.
          </p>
        </div>
      )}
    </div>
  );
}
```

### Multi-Step Form Progress

```tsx
import { useState } from "react";
import { ProgressLinear } from "@liquidify/components/progressLinear";

export default function MultiStepFormProgress() {
  const [currentStep, setCurrentStep] = useState(0);
  const [formData, setFormData] = useState({
    name: "",
    email: "",
    address: "",
    preferences: ""
  });

  const steps = [
    { title: "Personal Info", fields: ["name", "email"] },
    { title: "Address", fields: ["address"] },
    { title: "Preferences", fields: ["preferences"] }
  ];

  const totalSteps = steps.length;
  const progress = ((currentStep + 1) / totalSteps) * 100;

  const nextStep = () => {
    if (currentStep < totalSteps - 1) {
      setCurrentStep(currentStep + 1);
    }
  };

  const prevStep = () => {
    if (currentStep > 0) {
      setCurrentStep(currentStep - 1);
    }
  };

  const updateFormData = (field, value) => {
    setFormData(prev => ({ ...prev, [field]: value }));
  };

  const renderCurrentStep = () => {
    const step = steps[currentStep];

    return (
      <div style={{ marginTop: "2rem" }}>
        <h4>{step.title}</h4>

        {step.fields.map(field => (
          <div key={field} style={{ marginBottom: "1rem" }}>
            <label style={{ display: "block", marginBottom: "0.5rem", textTransform: "capitalize" }}>
              {field}
            </label>
            {field === "preferences" ? (
              <textarea
                value={formData[field]}
                onChange={(e) => updateFormData(field, e.target.value)}
                rows={4}
                style={{ width: "100%", padding: "0.5rem", border: "1px solid #ccc", borderRadius: "4px" }}
                placeholder="Tell us about your preferences..."
              />
            ) : (
              <input
                type={field === "email" ? "email" : "text"}
                value={formData[field]}
                onChange={(e) => updateFormData(field, e.target.value)}
                style={{ width: "100%", padding: "0.5rem", border: "1px solid #ccc", borderRadius: "4px" }}
                placeholder={`Enter your ${field}`}
              />
            )}
          </div>
        ))}
      </div>
    );
  };

  return (
    <div style={{ padding: "2rem", maxWidth: "600px", margin: "0 auto" }}>
      <h3>Registration Form</h3>

      <div style={{ marginBottom: "2rem" }}>
        <div style={{ display: "flex", justifyContent: "space-between", marginBottom: "0.5rem" }}>
          <span>Step {currentStep + 1} of {totalSteps}</span>
          <span>{Math.round(progress)}% Complete</span>
        </div>
        <ProgressLinear value={progress} />
      </div>

      <div style={{ marginBottom: "2rem" }}>
        <div style={{ display: "flex", justifyContent: "space-between", marginBottom: "1rem" }}>
          {steps.map((step, index) => (
            <div
              key={index}
              style={{
                flex: 1,
                textAlign: "center",
                padding: "0.5rem",
                backgroundColor: index <= currentStep ? "#007bff" : "#e9ecef",
                color: index <= currentStep ? "white" : "#666",
                borderRadius: index === 0 ? "4px 0 0 4px" : index === steps.length - 1 ? "0 4px 4px 0" : "0"
              }}
            >
              {step.title}
            </div>
          ))}
        </div>
      </div>

      {renderCurrentStep()}

      <div style={{ marginTop: "2rem", display: "flex", justifyContent: "space-between" }}>
        <button
          onClick={prevStep}
          disabled={currentStep === 0}
          style={{
            padding: "0.5rem 1rem",
            backgroundColor: currentStep === 0 ? "#6c757d" : "#6c757d",
            color: "white",
            border: "none",
            borderRadius: "4px",
            cursor: currentStep === 0 ? "not-allowed" : "pointer"
          }}
        >
          Previous
        </button>

        <button
          onClick={nextStep}
          disabled={currentStep === totalSteps - 1}
          style={{
            padding: "0.5rem 1rem",
            backgroundColor: currentStep === totalSteps - 1 ? "#28a745" : "#007bff",
            color: "white",
            border: "none",
            borderRadius: "4px",
            cursor: currentStep === totalSteps - 1 ? "pointer" : "pointer"
          }}
        >
          {currentStep === totalSteps - 1 ? "Submit" : "Next"}
        </button>
      </div>
    </div>
  );
}
```

### Loading States with Multiple Progress Bars

```tsx
import { useState, useEffect } from "react";
import { ProgressLinear } from "@liquidify/components/progressLinear";

export default function LoadingStates() {
  const [loadingStates, setLoadingStates] = useState({
    initializing: 0,
    connecting: 0,
    authenticating: 0,
    loadingData: 0
  });

  const [currentPhase, setCurrentPhase] = useState("initializing");

  useEffect(() => {
    const phases = ["initializing", "connecting", "authenticating", "loadingData"];

    phases.forEach((phase, index) => {
      setTimeout(() => {
        setCurrentPhase(phase);
        const interval = setInterval(() => {
          setLoadingStates(prev => {
            const newValue = prev[phase] + Math.random() * 15;
            if (newValue >= 100) {
              clearInterval(interval);
              return { ...prev, [phase]: 100 };
            }
            return { ...prev, [phase]: newValue };
          });
        }, 200);
      }, index * 1500);
    });
  }, []);

  const phaseLabels = {
    initializing: "Initializing application",
    connecting: "Connecting to server",
    authenticating: "Authenticating user",
    loadingData: "Loading user data"
  };

  const allComplete = Object.values(loadingStates).every(value => value === 100);

  return (
    <div style={{ padding: "2rem", maxWidth: "500px", margin: "0 auto" }}>
      <h3>Application Startup</h3>

      <div style={{ display: "flex", flexDirection: "column", gap: "1.5rem" }}>
        {Object.entries(loadingStates).map(([key, value]) => (
          <div key={key}>
            <div style={{ display: "flex", justifyContent: "space-between", marginBottom: "0.5rem" }}>
              <span style={{ fontWeight: currentPhase === key ? "bold" : "normal" }}>
                {phaseLabels[key]}
              </span>
              <span>{Math.round(value)}%</span>
            </div>
            <ProgressLinear
              value={value}
              size={currentPhase === key ? "lg" : "md"}
            />
          </div>
        ))}
      </div>

      {allComplete && (
        <div style={{ marginTop: "2rem", padding: "1rem", backgroundColor: "#d4edda", borderRadius: "8px", textAlign: "center" }}>
          <h4 style={{ color: "#155724", margin: 0 }}>🎉 Application Ready!</h4>
          <p style={{ color: "#155724", marginTop: "0.5rem" }}>
            All systems have been initialized successfully.
          </p>
        </div>
      )}
    </div>
  );
}
```

### Skill Level Indicators

```tsx
import { ProgressLinear } from "@liquidify/components/progressLinear";

export default function SkillLevelIndicators() {
  const skills = [
    { name: "JavaScript", level: 85, color: "#f7df1e" },
    { name: "React", level: 90, color: "#61dafb" },
    { name: "TypeScript", level: 75, color: "#3178c6" },
    { name: "Node.js", level: 70, color: "#339933" },
    { name: "Python", level: 65, color: "#3776ab" },
    { name: "CSS", level: 80, color: "#1572b6" }
  ];

  const getSkillLevel = (level) => {
    if (level >= 90) return "Expert";
    if (level >= 75) return "Advanced";
    if (level >= 60) return "Intermediate";
    if (level >= 40) return "Beginner";
    return "Novice";
  };

  return (
    <div style={{ padding: "2rem", maxWidth: "600px", margin: "0 auto" }}>
      <h3>Technical Skills</h3>

      <div style={{ display: "flex", flexDirection: "column", gap: "1.5rem" }}>
        {skills.map(skill => (
          <div key={skill.name}>
            <div style={{ display: "flex", justifyContent: "space-between", marginBottom: "0.5rem" }}>
              <span style={{ fontWeight: "bold" }}>{skill.name}</span>
              <span style={{ color: "#666" }}>
                {skill.level}% - {getSkillLevel(skill.level)}
              </span>
            </div>
            <ProgressLinear
              value={skill.level}
              style={{
                "--progress-linear-track-color": "#e9ecef",
                "--progress-linear-range-color": skill.color
              }}
            />
          </div>
        ))}
      </div>

      <div style={{ marginTop: "2rem", padding: "1rem", backgroundColor: "#f8f9fa", borderRadius: "8px" }}>
        <h4>Skill Level Guide</h4>
        <div style={{ display: "flex", flexWrap: "wrap", gap: "1rem", fontSize: "0.9rem" }}>
          <span><strong>90%+:</strong> Expert</span>
          <span><strong>75-89%:</strong> Advanced</span>
          <span><strong>60-74%:</strong> Intermediate</span>
          <span><strong>40-59%:</strong> Beginner</span>
          <span><strong>&lt;40%:</strong> Novice</span>
        </div>
      </div>
    </div>
  );
}
```

### Project Milestones

```tsx
import { ProgressLinear } from "@liquidify/components/progressLinear";

export default function ProjectMilestones() {
  const milestones = [
    {
      title: "Planning Phase",
      description: "Requirements gathering and project planning",
      progress: 100,
      status: "completed"
    },
    {
      title: "Design Phase",
      description: "UI/UX design and prototyping",
      progress: 100,
      status: "completed"
    },
    {
      title: "Development",
      description: "Core feature implementation",
      progress: 75,
      status: "in-progress"
    },
    {
      title: "Testing",
      description: "Quality assurance and bug fixing",
      progress: 30,
      status: "in-progress"
    },
    {
      title: "Deployment",
      description: "Production deployment and monitoring",
      progress: 0,
      status: "pending"
    }
  ];

  const overallProgress = milestones.reduce((acc, milestone) => acc + milestone.progress, 0) / milestones.length;

  const getStatusColor = (status) => {
    switch (status) {
      case "completed": return "#28a745";
      case "in-progress": return "#007bff";
      case "pending": return "#6c757d";
      default: return "#6c757d";
    }
  };

  return (
    <div style={{ padding: "2rem", maxWidth: "700px", margin: "0 auto" }}>
      <h3>Project Timeline</h3>

      <div style={{ marginBottom: "2rem" }}>
        <div style={{ display: "flex", justifyContent: "space-between", marginBottom: "0.5rem" }}>
          <span>Overall Progress</span>
          <span>{Math.round(overallProgress)}% Complete</span>
        </div>
        <ProgressLinear value={overallProgress} size="lg" />
      </div>

      <div style={{ display: "flex", flexDirection: "column", gap: "1.5rem" }}>
        {milestones.map((milestone, index) => (
          <div key={index} style={{
            padding: "1rem",
            border: "1px solid #dee2e6",
            borderRadius: "8px",
            backgroundColor: "white"
          }}>
            <div style={{ display: "flex", justifyContent: "space-between", marginBottom: "0.5rem" }}>
              <h4 style={{ margin: 0 }}>{milestone.title}</h4>
              <span style={{
                padding: "0.25rem 0.5rem",
                borderRadius: "12px",
                fontSize: "0.8rem",
                color: "white",
                backgroundColor: getStatusColor(milestone.status)
              }}>
                {milestone.status.replace("-", " ")}
              </span>
            </div>

            <p style={{ color: "#666", marginBottom: "1rem" }}>{milestone.description}</p>

            <div style={{ display: "flex", justifyContent: "space-between", marginBottom: "0.5rem" }}>
              <span>Progress</span>
              <span>{milestone.progress}%</span>
            </div>

            <ProgressLinear
              value={milestone.progress}
              style={{
                "--progress-linear-track-color": "#e9ecef",
                "--progress-linear-range-color": getStatusColor(milestone.status)
              }}
            />
          </div>
        ))}
      </div>
    </div>
  );
}
```

### Different Sizes

```tsx
import { ProgressLinear } from "@liquidify/components/progressLinear";

export default function DifferentSizes() {
  const sizes = ["xs", "sm", "md", "lg"];

  return (
    <div style={{ padding: "2rem", maxWidth: "600px", margin: "0 auto" }}>
      <h3>Progress Linear Sizes</h3>

      <div style={{ display: "flex", flexDirection: "column", gap: "2rem" }}>
        {sizes.map(size => (
          <div key={size}>
            <div style={{ display: "flex", justifyContent: "space-between", marginBottom: "0.5rem" }}>
              <span>Size: {size.toUpperCase()}</span>
              <span>75%</span>
            </div>
            <ProgressLinear value={75} size={size} />
          </div>
        ))}
      </div>

      <div style={{ marginTop: "3rem" }}>
        <h4>Size Comparison</h4>
        <div style={{ display: "flex", flexDirection: "column", gap: "0.5rem" }}>
          <ProgressLinear value={25} size="xs" />
          <ProgressLinear value={50} size="sm" />
          <ProgressLinear value={75} size="md" />
          <ProgressLinear value={100} size="lg" />
        </div>
      </div>
    </div>
  );
}
```

### With Custom Styling

```tsx
import { ProgressLinear } from "@liquidify/components/progressLinear";

export default function CustomStyledProgress() {
  return (
    <div style={{ padding: "2rem", maxWidth: "500px", margin: "0 auto" }}>
      <h3>Custom Styled Progress Bars</h3>

      <div style={{ display: "flex", flexDirection: "column", gap: "2rem", marginTop: "2rem" }}>
        <div>
          <h4>Success Progress</h4>
          <ProgressLinear
            value={85}
            label="Task completed successfully"
            style={{
              "--progress-linear-track-color": "#d4edda",
              "--progress-linear-range-color": "#28a745",
              "--progress-linear-track-height": "8px",
              borderRadius: "4px"
            }}
          />
        </div>

        <div>
          <h4>Warning Progress</h4>
          <ProgressLinear
            value={60}
            label="Processing with warnings"
            style={{
              "--progress-linear-track-color": "#fff3cd",
              "--progress-linear-range-color": "#ffc107",
              "--progress-linear-track-height": "6px"
            }}
          />
        </div>

        <div>
          <h4>Error Progress</h4>
          <ProgressLinear
            value={30}
            label="Error occurred during processing"
            style={{
              "--progress-linear-track-color": "#f8d7da",
              "--progress-linear-range-color": "#dc3545",
              "--progress-linear-track-height": "10px"
            }}
          />
        </div>

        <div>
          <h4>Info Progress</h4>
          <ProgressLinear
            value={45}
            label="Information being processed"
            style={{
              "--progress-linear-track-color": "#d1ecf1",
              "--progress-linear-range-color": "#17a2b8",
              "--progress-linear-track-height": "12px"
            }}
          />
        </div>
      </div>
    </div>
  );
}
```

## API Reference

### ProgressLinear

The linear progress bar component.

#### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `number` | `0` | The current progress value |
| `max` | `number` | `100` | The maximum progress value |
| `size` | `"xs" \| "sm" \| "md" \| "lg"` | `"md"` | The size/height of the progress bar |
| `label` | `string` | - | The label text to display |
| `children` | `ReactNode` | - | Custom content to display instead of label |
| All standard div props | - | - | Standard HTML div attributes |

## Accessibility

The ProgressLinear component includes comprehensive accessibility features:

- **ARIA Roles**: Implements proper progressbar role
- **ARIA Labels**: Automatic labeling for screen readers
- **Screen Reader Support**: Announces progress values and status
- **Keyboard Navigation**: Full keyboard support
- **High Contrast**: Compatible with high contrast themes

## Best Practices

1. **Clear Context**: Always provide context about what's being tracked
2. **Appropriate Size**: Choose size based on available space and importance
3. **Smooth Updates**: Update progress values smoothly to avoid jarring changes
4. **Completion Feedback**: Provide clear feedback when progress reaches 100%
5. **Loading States**: Use indeterminate progress for unknown durations
6. **Performance**: Avoid updating progress too frequently to prevent performance issues
7. **User Feedback**: Combine with text labels for better accessibility
8. **Consistent Styling**: Maintain consistent progress bar styling across your app
