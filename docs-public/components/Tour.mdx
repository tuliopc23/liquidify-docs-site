---
title: "Tour"
description: "Interactive guided tour component for onboarding and feature discovery"
---

# Tour

An interactive guided tour component that helps users discover features and navigate through your application. Perfect for onboarding new users, introducing new features, or providing contextual help with step-by-step guidance.

## Installation

```bash
npm install @liquidify/components
```

## Import

```tsx
import { Tour } from "@liquidify/components/tour";
```

## Usage

### Basic Tour

```tsx
import { useState } from "react";
import { Tour } from "@liquidify/components/tour";

export default function BasicTour() {
  const [isOpen, setIsOpen] = useState(false);
  const [currentStep, setCurrentStep] = useState(0);

  const steps = [
    {
      target: "#step1",
      title: "Welcome!",
      description: "This is your dashboard where you can see all your projects."
    },
    {
      target: "#step2", 
      title: "Create New",
      description: "Click here to create a new project or item."
    },
    {
      target: "#step3",
      title: "Settings",
      description: "Access your account settings and preferences here."
    }
  ];

  return (
    <div>
      <button onClick={() => setIsOpen(true)}>
        Start Tour
      </button>
      
      <div style={{ padding: "2rem", display: "flex", gap: "2rem" }}>
        <div id="step1" style={{ padding: "1rem", border: "1px solid #ccc" }}>
          Dashboard Content
        </div>
        <div id="step2" style={{ padding: "1rem", border: "1px solid #ccc" }}>
          Create Button
        </div>
        <div id="step3" style={{ padding: "1rem", border: "1px solid #ccc" }}>
          Settings Menu
        </div>
      </div>

      <Tour.Root 
        open={isOpen} 
        onOpenChange={setIsOpen}
        step={currentStep}
        onStepChange={setCurrentStep}
      >
        <Tour.Spotlight />
        <Tour.Positioner>
          <Tour.Content>
            <Tour.Arrow>
              <Tour.ArrowTip />
            </Tour.Arrow>
            
            <Tour.Title>{steps[currentStep]?.title}</Tour.Title>
            <Tour.Description>{steps[currentStep]?.description}</Tour.Description>
            
            <div style={{ display: "flex", justifyContent: "space-between", marginTop: "1rem" }}>
              <button 
                onClick={() => setCurrentStep(Math.max(0, currentStep - 1))}
                disabled={currentStep === 0}
              >
                Previous
              </button>
              
              <span>{currentStep + 1} of {steps.length}</span>
              
              {currentStep < steps.length - 1 ? (
                <button onClick={() => setCurrentStep(currentStep + 1)}>
                  Next
                </button>
              ) : (
                <button onClick={() => setIsOpen(false)}>
                  Finish
                </button>
              )}
            </div>
            
            <Tour.CloseTrigger>×</Tour.CloseTrigger>
          </Tour.Content>
        </Tour.Positioner>
      </Tour.Root>
    </div>
  );
}
```

### Product Feature Tour

```tsx
import { useState } from "react";
import { Tour } from "@liquidify/components/tour";

export default function ProductFeatureTour() {
  const [tourOpen, setTourOpen] = useState(false);
  const [step, setStep] = useState(0);

  const tourSteps = [
    {
      title: "File Explorer",
      description: "Browse and manage all your files in this sidebar. You can create folders, upload files, and organize your workspace.",
      target: "#file-explorer"
    },
    {
      title: "Code Editor", 
      description: "Write and edit your code here. The editor supports syntax highlighting, auto-completion, and real-time error detection.",
      target: "#code-editor"
    },
    {
      title: "Preview Panel",
      description: "See a live preview of your changes instantly. No need to refresh - updates happen automatically as you type.",
      target: "#preview-panel"
    },
    {
      title: "Terminal",
      description: "Run commands, install packages, and view build outputs directly in the integrated terminal.",
      target: "#terminal"
    },
    {
      title: "Collaboration",
      description: "Share your workspace with teammates. See their cursors in real-time and collaborate seamlessly.",
      target: "#collaboration"
    }
  ];

  return (
    <div>
      <header style={{ padding: "1rem", borderBottom: "1px solid #ccc", display: "flex", justifyContent: "space-between" }}>
        <h1>Code Editor</h1>
        <div>
          <button onClick={() => setTourOpen(true)}>Take Tour</button>
          <span id="collaboration" style={{ marginLeft: "1rem" }}>👥 Collaborate</span>
        </div>
      </header>
      
      <div style={{ display: "flex", height: "500px" }}>
        <div id="file-explorer" style={{ width: "200px", background: "#f5f5f5", padding: "1rem" }}>
          <h3>Files</h3>
          <ul>
            <li>📄 index.html</li>
            <li>📄 style.css</li>
            <li>📄 script.js</li>
          </ul>
        </div>
        
        <div id="code-editor" style={{ flex: 1, padding: "1rem", background: "#1e1e1e", color: "white" }}>
          <h3>Code Editor</h3>
          <pre style={{ color: "#ddd" }}>
{`function hello() {
  console.log("Hello, World!");
}`}
          </pre>
        </div>
        
        <div id="preview-panel" style={{ width: "300px", padding: "1rem", background: "#fff" }}>
          <h3>Preview</h3>
          <p>Your preview appears here...</p>
        </div>
      </div>
      
      <div id="terminal" style={{ height: "100px", background: "#000", color: "#00ff00", padding: "1rem" }}>
        <div>Terminal $ npm start</div>
        <div>Server running on port 3000...</div>
      </div>

      <Tour.Root 
        open={tourOpen} 
        onOpenChange={setTourOpen}
        step={step}
        onStepChange={setStep}
      >
        <Tour.Spotlight />
        <Tour.Positioner>
          <Tour.Content>
            <Tour.Arrow>
              <Tour.ArrowTip />
            </Tour.Arrow>
            
            <Tour.Title>{tourSteps[step]?.title}</Tour.Title>
            <Tour.Description>{tourSteps[step]?.description}</Tour.Description>
            
            <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginTop: "1rem" }}>
              <button 
                onClick={() => setStep(Math.max(0, step - 1))}
                disabled={step === 0}
              >
                ← Back
              </button>
              
              <div style={{ display: "flex", gap: "4px" }}>
                {tourSteps.map((_, index) => (
                  <div
                    key={index}
                    style={{
                      width: "8px",
                      height: "8px",
                      borderRadius: "50%",
                      background: index === step ? "#007bff" : "#ccc"
                    }}
                  />
                ))}
              </div>
              
              <Tour.ActionTrigger
                onClick={() => {
                  if (step < tourSteps.length - 1) {
                    setStep(step + 1);
                  } else {
                    setTourOpen(false);
                  }
                }}
              >
                {step < tourSteps.length - 1 ? "Next →" : "Get Started"}
              </Tour.ActionTrigger>
            </div>
            
            <Tour.CloseTrigger 
              style={{ 
                position: "absolute", 
                top: "8px", 
                right: "8px",
                background: "none",
                border: "none",
                fontSize: "18px",
                cursor: "pointer"
              }}
            >
              ×
            </Tour.CloseTrigger>
          </Tour.Content>
        </Tour.Positioner>
      </Tour.Root>
    </div>
  );
}
```

### Conditional Tour Steps

```tsx
import { useState } from "react";
import { Tour } from "@liquidify/components/tour";

export default function ConditionalTour() {
  const [tourOpen, setTourOpen] = useState(false);
  const [step, setStep] = useState(0);
  const [userType, setUserType] = useState("beginner");

  const getStepsForUser = (type) => {
    const commonSteps = [
      { target: "#welcome", title: "Welcome!", description: "Welcome to our platform!" }
    ];

    const beginnerSteps = [
      { target: "#tutorial", title: "Start Here", description: "Check out our beginner tutorial." },
      { target: "#help", title: "Need Help?", description: "Access help documentation anytime." }
    ];

    const advancedSteps = [
      { target: "#advanced", title: "Advanced Features", description: "Explore powerful advanced tools." },
      { target: "#api", title: "API Access", description: "Integrate with our API for custom solutions." }
    ];

    return [
      ...commonSteps,
      ...(type === "beginner" ? beginnerSteps : advancedSteps)
    ];
  };

  const currentSteps = getStepsForUser(userType);

  return (
    <div>
      <div style={{ marginBottom: "1rem" }}>
        <label>
          User Type: 
          <select value={userType} onChange={(e) => setUserType(e.target.value)}>
            <option value="beginner">Beginner</option>
            <option value="advanced">Advanced</option>
          </select>
        </label>
        <button onClick={() => setTourOpen(true)} style={{ marginLeft: "1rem" }}>
          Start Personalized Tour
        </button>
      </div>

      <div style={{ padding: "2rem", display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(200px, 1fr))", gap: "1rem" }}>
        <div id="welcome" style={{ padding: "1rem", border: "1px solid #ccc" }}>
          Welcome Section
        </div>
        <div id="tutorial" style={{ padding: "1rem", border: "1px solid #ccc" }}>
          Tutorial Section
        </div>
        <div id="help" style={{ padding: "1rem", border: "1px solid #ccc" }}>
          Help Center
        </div>
        <div id="advanced" style={{ padding: "1rem", border: "1px solid #ccc" }}>
          Advanced Tools
        </div>
        <div id="api" style={{ padding: "1rem", border: "1px solid #ccc" }}>
          API Documentation
        </div>
      </div>

      <Tour.Root 
        open={tourOpen} 
        onOpenChange={setTourOpen}
        step={step}
        onStepChange={setStep}
      >
        <Tour.Spotlight />
        <Tour.Positioner>
          <Tour.Content>
            <Tour.Arrow>
              <Tour.ArrowTip />
            </Tour.Arrow>
            
            <div style={{ padding: "1rem" }}>
              <Tour.Title style={{ marginBottom: "0.5rem", fontSize: "1.2rem", fontWeight: "bold" }}>
                {currentSteps[step]?.title}
              </Tour.Title>
              
              <Tour.Description style={{ marginBottom: "1rem", color: "#666" }}>
                {currentSteps[step]?.description}
              </Tour.Description>
              
              <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center" }}>
                <button 
                  onClick={() => setStep(Math.max(0, step - 1))}
                  disabled={step === 0}
                  style={{ padding: "0.5rem 1rem" }}
                >
                  Previous
                </button>
                
                <span style={{ fontSize: "0.9rem", color: "#888" }}>
                  {step + 1} of {currentSteps.length}
                </span>
                
                <button
                  onClick={() => {
                    if (step < currentSteps.length - 1) {
                      setStep(step + 1);
                    } else {
                      setTourOpen(false);
                      setStep(0);
                    }
                  }}
                  style={{ padding: "0.5rem 1rem", backgroundColor: "#007bff", color: "white", border: "none", borderRadius: "4px" }}
                >
                  {step < currentSteps.length - 1 ? "Next" : "Finish"}
                </button>
              </div>
            </div>
            
            <Tour.CloseTrigger 
              style={{ 
                position: "absolute", 
                top: "8px", 
                right: "8px",
                background: "none",
                border: "none",
                fontSize: "16px",
                cursor: "pointer"
              }}
            >
              ✕
            </Tour.CloseTrigger>
          </Tour.Content>
        </Tour.Positioner>
      </Tour.Root>
    </div>
  );
}
```

## API Reference

### Tour.Root

The root container for the tour component.

#### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `open` | `boolean` | - | Whether the tour is visible |
| `onOpenChange` | `(open: boolean) => void` | - | Callback fired when visibility changes |
| `step` | `number` | `0` | Current step index |
| `onStepChange` | `(step: number) => void` | - | Callback fired when step changes |
| `steps` | `TourStep[]` | - | Array of tour step configurations |

### Tour.Positioner

The positioning wrapper for tour content.

#### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `children` | `ReactNode` | - | The positioner content |

### Tour.Content

The main content container for the tour step.

#### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `children` | `ReactNode` | - | The tour content |

### Tour.Title

The title of the current tour step.

#### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `children` | `ReactNode` | - | The title content |

### Tour.Description

The description text for the current tour step.

#### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `children` | `ReactNode` | - | The description content |

### Tour.ActionTrigger

A button for tour actions (next, previous, finish).

#### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `onClick` | `() => void` | - | Action click handler |
| `children` | `ReactNode` | - | The action trigger content |

### Tour.CloseTrigger

A button to close/exit the tour.

#### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `children` | `ReactNode` | - | The close trigger content |

### Tour.Spotlight

A visual spotlight effect that highlights the target element.

#### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| All standard div props | - | - | Standard HTML div attributes |

### Tour.Arrow

The pointing arrow for the tour content.

#### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `children` | `ReactNode` | - | The arrow content (usually ArrowTip) |

### Tour.ArrowTip

The tip/point of the tour arrow.

#### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| All standard div props | - | - | Standard HTML div attributes |

## Accessibility

The Tour component includes comprehensive accessibility features:

- **ARIA Labels**: Implements proper ARIA roles and labels for tour elements
- **Keyboard Navigation**: Full keyboard support for navigating through steps
- **Focus Management**: Manages focus appropriately during tour progression
- **Screen Reader Support**: Announces tour content and step changes
- **High Contrast**: Compatible with high contrast themes

### Keyboard Shortcuts

| Key | Action |
|-----|--------|
| `Escape` | Close the tour |
| `Tab` | Navigate between tour controls |
| `Enter` / `Space` | Activate focused button |
| `Arrow Left` | Previous step (when applicable) |
| `Arrow Right` | Next step (when applicable) |

## Best Practices

1. **Clear Progression**: Make it obvious how many steps remain and current progress
2. **Concise Content**: Keep step titles and descriptions brief and focused
3. **Visual Hierarchy**: Use proper heading levels and clear visual structure
4. **Skip Option**: Always provide a way to skip or exit the tour
5. **Responsive Design**: Ensure tours work well on all screen sizes
6. **Smart Positioning**: Position tour content to avoid covering important elements
7. **Progressive Disclosure**: Introduce features gradually, not all at once
8. **Contextual Timing**: Show tours at the right moment in the user journey
