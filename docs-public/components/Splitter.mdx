---
title: "Splitter"
description: "Resizable panel layout component for creating adjustable split views"
---

# Splitter

A resizable panel layout component that allows users to adjust the size of adjacent panels by dragging resize handles. Perfect for code editors, file explorers, comparison views, or any interface requiring flexible panel layouts with user-controlled sizing.

## Installation

```bash
npm install @liquidify/components
```

## Import

```tsx
import { Splitter } from "@liquidify/components/splitter";
```

## Usage

### Basic Horizontal Split

```tsx
import { Splitter } from "@liquidify/components/splitter";

export default function BasicHorizontalSplit() {
  return (
    <div style={{ height: "400px", border: "1px solid #ccc" }}>
      <Splitter.Root orientation="horizontal">
        <Splitter.Panel>
          <div style={{ padding: "1rem", height: "100%", backgroundColor: "#f8f9fa" }}>
            <h3>Left Panel</h3>
            <p>This is the left panel content. You can resize it by dragging the handle.</p>
            <ul>
              <li>Item 1</li>
              <li>Item 2</li>
              <li>Item 3</li>
            </ul>
          </div>
        </Splitter.Panel>

        <Splitter.ResizeTrigger />

        <Splitter.Panel>
          <div style={{ padding: "1rem", height: "100%", backgroundColor: "#e9ecef" }}>
            <h3>Right Panel</h3>
            <p>This is the right panel content. It adjusts automatically when you resize.</p>
            <div style={{ marginTop: "1rem" }}>
              <div style={{ width: "100%", height: "200px", backgroundColor: "#dee2e6", display: "flex", alignItems: "center", justifyContent: "center" }}>
                Content Area
              </div>
            </div>
          </div>
        </Splitter.Panel>
      </Splitter.Root>
    </div>
  );
}
```

### Code Editor Layout

```tsx
import { Splitter } from "@liquidify/components/splitter";

export default function CodeEditorLayout() {
  return (
    <div style={{ height: "600px", border: "1px solid #ccc" }}>
      <Splitter.Root orientation="horizontal">
        <Splitter.Panel defaultSize={20} minSize={15}>
          <div style={{ height: "100%", backgroundColor: "#2d3748", color: "white", padding: "1rem" }}>
            <h4 style={{ marginTop: 0, color: "#e2e8f0" }}>File Explorer</h4>
            <div style={{ fontFamily: "monospace", fontSize: "14px" }}>
              <div>📁 src/</div>
              <div style={{ marginLeft: "1rem" }}>📄 index.tsx</div>
              <div style={{ marginLeft: "1rem" }}>📄 App.tsx</div>
              <div style={{ marginLeft: "1rem" }}>📄 styles.css</div>
              <div>📁 public/</div>
              <div style={{ marginLeft: "1rem" }}>📄 index.html</div>
              <div>📄 package.json</div>
            </div>
          </div>
        </Splitter.Panel>

        <Splitter.ResizeTrigger />

        <Splitter.Panel defaultSize={60}>
          <Splitter.Root orientation="vertical">
            <Splitter.Panel>
              <div style={{ height: "100%", backgroundColor: "#1a202c", color: "#e2e8f0", padding: "1rem" }}>
                <div style={{ display: "flex", justifyContent: "space-between", marginBottom: "1rem" }}>
                  <span>App.tsx</span>
                  <span style={{ color: "#68d391" }}>●</span>
                </div>
                <pre style={{ fontFamily: "monospace", fontSize: "14px", margin: 0 }}>
{`import React from 'react';
import './App.css';

function App() {
  return (
    <div className="App">
      <header className="App-header">
        <h1>Hello, World!</h1>
      </header>
    </div>
  );
}

export default App;`}
                </pre>
              </div>
            </Splitter.Panel>

            <Splitter.ResizeTrigger />

            <Splitter.Panel defaultSize={30} minSize={20}>
              <div style={{ height: "100%", backgroundColor: "#2d3748", color: "#e2e8f0", padding: "1rem" }}>
                <h4 style={{ marginTop: 0, color: "#e2e8f0" }}>Terminal</h4>
                <div style={{ fontFamily: "monospace", fontSize: "14px" }}>
                  <div>$ npm start</div>
                  <div style={{ color: "#68d391" }}>Compiled successfully!</div>
                  <div style={{ color: "#4299e1" }}>You can now view your app in the browser.</div>
                  <div style={{ color: "#4299e1" }}>Local: http://localhost:3000</div>
                  <div>$ █</div>
                </div>
              </div>
            </Splitter.Panel>
          </Splitter.Root>
        </Splitter.Panel>

        <Splitter.ResizeTrigger />

        <Splitter.Panel defaultSize={20} minSize={15}>
          <div style={{ height: "100%", backgroundColor: "#f7fafc", padding: "1rem" }}>
            <h4 style={{ marginTop: 0 }}>Preview</h4>
            <div style={{ border: "1px solid #e2e8f0", borderRadius: "4px", padding: "1rem", backgroundColor: "white" }}>
              <h1 style={{ color: "#2d3748" }}>Hello, World!</h1>
            </div>
          </div>
        </Splitter.Panel>
      </Splitter.Root>
    </div>
  );
}
```

### Vertical Split Layout

```tsx
import { Splitter } from "@liquidify/components/splitter";

export default function VerticalSplitLayout() {
  return (
    <div style={{ height: "500px", border: "1px solid #ccc" }}>
      <Splitter.Root orientation="vertical">
        <Splitter.Panel defaultSize={60}>
          <div style={{ padding: "1rem", height: "100%", backgroundColor: "#f8f9fa" }}>
            <h3>Main Content Area</h3>
            <p>This is the primary content area that takes up most of the space.</p>
            <div style={{ marginTop: "1rem", padding: "1rem", backgroundColor: "white", border: "1px solid #dee2e6", borderRadius: "4px" }}>
              <h4>Article Title</h4>
              <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.</p>
              <p>Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat.</p>
            </div>
          </div>
        </Splitter.Panel>

        <Splitter.ResizeTrigger />

        <Splitter.Panel defaultSize={40} minSize={25}>
          <div style={{ padding: "1rem", height: "100%", backgroundColor: "#e9ecef" }}>
            <h3>Sidebar</h3>
            <div style={{ marginTop: "1rem" }}>
              <h4>Related Articles</h4>
              <ul style={{ paddingLeft: "1.5rem" }}>
                <li><a href="#" style={{ color: "#007bff", textDecoration: "none" }}>How to use React Hooks</a></li>
                <li><a href="#" style={{ color: "#007bff", textDecoration: "none" }}>Understanding State Management</a></li>
                <li><a href="#" style={{ color: "#007bff", textDecoration: "none" }}>Best Practices for Components</a></li>
              </ul>
            </div>
            <div style={{ marginTop: "2rem" }}>
              <h4>Tags</h4>
              <div style={{ display: "flex", flexWrap: "wrap", gap: "0.5rem" }}>
                <span style={{ padding: "0.25rem 0.5rem", backgroundColor: "#007bff", color: "white", borderRadius: "12px", fontSize: "0.8rem" }}>React</span>
                <span style={{ padding: "0.25rem 0.5rem", backgroundColor: "#28a745", color: "white", borderRadius: "12px", fontSize: "0.8rem" }}>JavaScript</span>
                <span style={{ padding: "0.25rem 0.5rem", backgroundColor: "#6c757d", color: "white", borderRadius: "12px", fontSize: "0.8rem" }}>Web Dev</span>
              </div>
            </div>
          </div>
        </Splitter.Panel>
      </Splitter.Root>
    </div>
  );
}
```

### Comparison View

```tsx
import { useState } from "react";
import { Splitter } from "@liquidify/components/splitter";

export default function ComparisonView() {
  const [splitPosition, setSplitPosition] = useState(50);

  const beforeCode = `function greet(name) {
  console.log("Hello, " + name);
}

greet("World");`;

  const afterCode = `function greet(name) {
  const message = \`Hello, \${name}!\`;
  console.log(message);
  return message;
}

const result = greet("World");
console.log("Result:", result);`;

  return (
    <div style={{ height: "400px", border: "1px solid #ccc" }}>
      <div style={{ padding: "1rem", backgroundColor: "#f8f9fa", borderBottom: "1px solid #dee2e6" }}>
        <h3>Code Comparison</h3>
        <p>Compare before and after versions by adjusting the splitter</p>
      </div>

      <Splitter.Root orientation="horizontal">
        <Splitter.Panel>
          <div style={{ padding: "1rem", height: "100%", backgroundColor: "#fff3cd", borderRight: "2px solid #ffc107" }}>
            <h4 style={{ color: "#856404", marginTop: 0 }}>Before</h4>
            <pre style={{ backgroundColor: "#fff", padding: "1rem", borderRadius: "4px", margin: 0, fontFamily: "monospace", fontSize: "14px" }}>
              {beforeCode}
            </pre>
          </div>
        </Splitter.Panel>

        <Splitter.ResizeTrigger />

        <Splitter.Panel>
          <div style={{ padding: "1rem", height: "100%", backgroundColor: "#d4edda", borderLeft: "2px solid #28a745" }}>
            <h4 style={{ color: "#155724", marginTop: 0 }}>After</h4>
            <pre style={{ backgroundColor: "#fff", padding: "1rem", borderRadius: "4px", margin: 0, fontFamily: "monospace", fontSize: "14px" }}>
              {afterCode}
            </pre>
          </div>
        </Splitter.Panel>
      </Splitter.Root>
    </div>
  );
}
```

### Three Panel Layout

```tsx
import { Splitter } from "@liquidify/components/splitter";

export default function ThreePanelLayout() {
  return (
    <div style={{ height: "500px", border: "1px solid #ccc" }}>
      <Splitter.Root orientation="horizontal">
        <Splitter.Panel defaultSize={25} minSize={20}>
          <div style={{ padding: "1rem", height: "100%", backgroundColor: "#e3f2fd" }}>
            <h4 style={{ marginTop: 0 }}>Navigation</h4>
            <nav>
              <ul style={{ listStyle: "none", padding: 0 }}>
                <li style={{ marginBottom: "0.5rem" }}>
                  <a href="#" style={{ color: "#1976d2", textDecoration: "none" }}>Dashboard</a>
                </li>
                <li style={{ marginBottom: "0.5rem" }}>
                  <a href="#" style={{ color: "#1976d2", textDecoration: "none" }}>Projects</a>
                </li>
                <li style={{ marginBottom: "0.5rem" }}>
                  <a href="#" style={{ color: "#1976d2", textDecoration: "none" }}>Team</a>
                </li>
                <li style={{ marginBottom: "0.5rem" }}>
                  <a href="#" style={{ color: "#1976d2", textDecoration: "none" }}>Settings</a>
                </li>
              </ul>
            </nav>
          </div>
        </Splitter.Panel>

        <Splitter.ResizeTrigger />

        <Splitter.Panel defaultSize={50}>
          <Splitter.Root orientation="vertical">
            <Splitter.Panel defaultSize={70}>
              <div style={{ padding: "1rem", height: "100%", backgroundColor: "#f8f9fa" }}>
                <h3 style={{ marginTop: 0 }}>Main Content</h3>
                <div style={{ marginBottom: "1rem" }}>
                  <h4>Project Overview</h4>
                  <p>This is the main content area where you can view and edit your project details.</p>
                </div>
                <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(200px, 1fr))", gap: "1rem" }}>
                  <div style={{ padding: "1rem", backgroundColor: "white", border: "1px solid #dee2e6", borderRadius: "4px" }}>
                    <h5>Task 1</h5>
                    <p>Complete the user interface design</p>
                  </div>
                  <div style={{ padding: "1rem", backgroundColor: "white", border: "1px solid #dee2e6", borderRadius: "4px" }}>
                    <h5>Task 2</h5>
                    <p>Implement the backend API</p>
                  </div>
                  <div style={{ padding: "1rem", backgroundColor: "white", border: "1px solid #dee2e6", borderRadius: "4px" }}>
                    <h5>Task 3</h5>
                    <p>Write unit tests</p>
                  </div>
                </div>
              </div>
            </Splitter.Panel>

            <Splitter.ResizeTrigger />

            <Splitter.Panel defaultSize={30} minSize={20}>
              <div style={{ padding: "1rem", height: "100%", backgroundColor: "#fff3cd" }}>
                <h4 style={{ marginTop: 0 }}>Activity Feed</h4>
                <div style={{ display: "flex", flexDirection: "column", gap: "0.5rem" }}>
                  <div style={{ padding: "0.5rem", backgroundColor: "white", borderRadius: "4px" }}>
                    <small style={{ color: "#6c757d" }}>2 hours ago</small>
                    <p style={{ margin: "0.25rem 0" }}>John updated the project timeline</p>
                  </div>
                  <div style={{ padding: "0.5rem", backgroundColor: "white", borderRadius: "4px" }}>
                    <small style={{ color: "#6c757d" }}>4 hours ago</small>
                    <p style={{ margin: "0.25rem 0" }}>Sarah completed the design review</p>
                  </div>
                  <div style={{ padding: "0.5rem", backgroundColor: "white", borderRadius: "4px" }}>
                    <small style={{ color: "#6c757d" }}>1 day ago</small>
                    <p style={{ margin: "0.25rem 0" }}>Mike added new team members</p>
                  </div>
                </div>
              </div>
            </Splitter.Panel>
          </Splitter.Root>
        </Splitter.Panel>

        <Splitter.ResizeTrigger />

        <Splitter.Panel defaultSize={25} minSize={20}>
          <div style={{ padding: "1rem", height: "100%", backgroundColor: "#f8d7da" }}>
            <h4 style={{ marginTop: 0 }}>Details</h4>
            <div style={{ marginBottom: "1rem" }}>
              <h5>Project Info</h5>
              <p><strong>Status:</strong> In Progress</p>
              <p><strong>Due Date:</strong> Dec 31, 2024</p>
              <p><strong>Team:</strong> 5 members</p>
            </div>
            <div>
              <h5>Quick Actions</h5>
              <div style={{ display: "flex", flexDirection: "column", gap: "0.5rem" }}>
                <button style={{ padding: "0.5rem", backgroundColor: "#007bff", color: "white", border: "none", borderRadius: "4px" }}>
                  Add Task
                </button>
                <button style={{ padding: "0.5rem", backgroundColor: "#28a745", color: "white", border: "none", borderRadius: "4px" }}>
                  Share Project
                </button>
                <button style={{ padding: "0.5rem", backgroundColor: "#6c757d", color: "white", border: "none", borderRadius: "4px" }}>
                  Export Data
                </button>
              </div>
            </div>
          </div>
        </Splitter.Panel>
      </Splitter.Root>
    </div>
  );
}
```

### File Explorer with Preview

```tsx
import { useState } from "react";
import { Splitter } from "@liquidify/components/splitter";

export default function FileExplorerWithPreview() {
  const [selectedFile, setSelectedFile] = useState("readme");

  const files = {
    readme: { name: "README.md", content: "# Project Documentation\n\nThis is a sample project with multiple files." },
    config: { name: "config.json", content: '{\n  "version": "1.0.0",\n  "port": 3000,\n  "database": "mongodb"\n}' },
    script: { name: "app.js", content: 'const express = require("express");\nconst app = express();\n\napp.get("/", (req, res) => {\n  res.send("Hello World!");\n});\n\napp.listen(3000);' }
  };

  return (
    <div style={{ height: "500px", border: "1px solid #ccc" }}>
      <Splitter.Root orientation="horizontal">
        <Splitter.Panel defaultSize={30} minSize={25}>
          <div style={{ padding: "1rem", height: "100%", backgroundColor: "#f8f9fa" }}>
            <h4 style={{ marginTop: 0 }}>Files</h4>
            <div style={{ display: "flex", flexDirection: "column", gap: "0.25rem" }}>
              {Object.entries(files).map(([key, file]) => (
                <div
                  key={key}
                  onClick={() => setSelectedFile(key)}
                  style={{
                    padding: "0.5rem",
                    cursor: "pointer",
                    backgroundColor: selectedFile === key ? "#007bff" : "transparent",
                    color: selectedFile === key ? "white" : "inherit",
                    borderRadius: "4px"
                  }}
                >
                  📄 {file.name}
                </div>
              ))}
            </div>
          </div>
        </Splitter.Panel>

        <Splitter.ResizeTrigger />

        <Splitter.Panel>
          <div style={{ padding: "1rem", height: "100%", backgroundColor: "#2d3748", color: "#e2e8f0" }}>
            <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: "1rem" }}>
              <h4 style={{ margin: 0 }}>{files[selectedFile].name}</h4>
              <span style={{ fontSize: "0.8rem", color: "#68d391" }}>●</span>
            </div>
            <pre style={{
              fontFamily: "monospace",
              fontSize: "14px",
              margin: 0,
              whiteSpace: "pre-wrap",
              wordWrap: "break-word"
            }}>
              {files[selectedFile].content}
            </pre>
          </div>
        </Splitter.Panel>
      </Splitter.Root>
    </div>
  );
}
```

### Splitter with Size Constraints

```tsx
import { Splitter } from "@liquidify/components/splitter";

export default function SplitterWithConstraints() {
  return (
    <div style={{ height: "400px", border: "1px solid #ccc" }}>
      <Splitter.Root orientation="horizontal">
        <Splitter.Panel defaultSize={30} minSize={20} maxSize={50}>
          <div style={{ padding: "1rem", height: "100%", backgroundColor: "#e3f2fd" }}>
            <h4 style={{ marginTop: 0 }}>Fixed Width Panel</h4>
            <p>This panel has size constraints:</p>
            <ul>
              <li>Default: 30%</li>
              <li>Minimum: 20%</li>
              <li>Maximum: 50%</li>
            </ul>
            <p>Try dragging the resize handle to see the constraints in action.</p>
          </div>
        </Splitter.Panel>

        <Splitter.ResizeTrigger />

        <Splitter.Panel defaultSize={40} minSize={30}>
          <div style={{ padding: "1rem", height: "100%", backgroundColor: "#f3e5f5" }}>
            <h4 style={{ marginTop: 0 }}>Flexible Panel</h4>
            <p>This panel can grow larger but has a minimum size of 30%.</p>
            <div style={{ marginTop: "1rem", padding: "1rem", backgroundColor: "white", borderRadius: "4px" }}>
              <p>The third panel will automatically adjust to fill the remaining space.</p>
            </div>
          </div>
        </Splitter.Panel>

        <Splitter.ResizeTrigger />

        <Splitter.Panel defaultSize={30} minSize={20} maxSize={40}>
          <div style={{ padding: "1rem", height: "100%", backgroundColor: "#e8f5e8" }}>
            <h4 style={{ marginTop: 0 }}>Constrained Panel</h4>
            <p>This panel also has size limits:</p>
            <ul>
              <li>Default: 30%</li>
              <li>Minimum: 20%</li>
              <li>Maximum: 40%</li>
            </ul>
            <p>The constraints prevent any panel from becoming too small or too large.</p>
          </div>
        </Splitter.Panel>
      </Splitter.Root>
    </div>
  );
}
```

## API Reference

### Splitter.Root

The root container for the splitter component.

#### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `orientation` | `"horizontal" \| "vertical"` | `"horizontal"` | The orientation of the splitter |
| `size` | `number[]` | - | Controlled sizes of the panels |
| `onSizeChange` | `(sizes: number[]) => void` | - | Callback fired when panel sizes change |
| `children` | `ReactNode` | - | The splitter content |

### Splitter.Panel

An individual resizable panel within the splitter.

#### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `defaultSize` | `number` | - | Default size of the panel (as percentage) |
| `minSize` | `number` | - | Minimum size of the panel (as percentage) |
| `maxSize` | `number` | - | Maximum size of the panel (as percentage) |
| `children` | `ReactNode` | - | The panel content |

### Splitter.ResizeTrigger

The draggable handle for resizing panels.

#### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `disabled` | `boolean` | `false` | Whether the resize trigger is disabled |
| `children` | `ReactNode` | - | Custom trigger content |

## Accessibility

The Splitter component includes comprehensive accessibility features:

- **ARIA Roles**: Implements proper separator and adjustable roles
- **Keyboard Navigation**: Full keyboard support for resizing panels
- **Screen Reader Support**: Announces resize actions and panel information
- **Focus Management**: Proper focus handling for resize handles
- **High Contrast**: Compatible with high contrast themes

### Keyboard Shortcuts

| Key | Action |
|-----|--------|
| `Tab` | Move focus to resize handle |
| `Enter` / `Space` | Activate resize mode |
| `Arrow Keys` | Resize panels when in resize mode |
| `Escape` | Exit resize mode |

## Best Practices

1. **Size Constraints**: Always set appropriate min/max sizes to prevent panels from becoming unusable
2. **Visual Feedback**: Provide clear visual indicators for resize handles
3. **Default Sizes**: Set sensible default sizes based on content importance
4. **Responsive Design**: Consider how splitters behave on different screen sizes
5. **Performance**: Avoid putting heavy content in frequently resized panels
6. **Persistence**: Consider saving user panel sizes for better UX
7. **Touch Support**: Ensure resize handles are touch-friendly on mobile devices
8. **Loading States**: Handle loading states gracefully when panel content is dynamic
